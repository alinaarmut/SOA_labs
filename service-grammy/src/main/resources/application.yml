spring:
  application:
    name: service-grammy
  profiles:
    active: dev
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      fail-fast: false # если true приложение упадет если не будет конфига
  datasource:
    url: jdbc:postgresql://localhost:5432/studs
    username: s367826
    password: 4Xb2gijcYKqkn0xt
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
cxf:
  path: /services
  servlet:
    init:
      service-list-path: /info
grammy:
  mule:
    soap-address: http://localhost:8082/integration
    wsdl: http://localhost:8082/integration?wsdl
server:
  port: ${PORT:8444} # фикс порт для адаптера
#  port: 0
  ssl:
    enabled: false
#cloud:
#  consul:
#    host: localhost
#    port: 8500
#    discovery:
#      enabled: true
#      register: false
#    config:
#      enabled: false
#  service-registry:
#    auto-registration:
#      enabled: false


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    health-check-url-path: /actuator/health

# LoadBalancer
spring.cloud.loadbalancer.enabled: false
ribbon:
  eureka:
    enabled: false
