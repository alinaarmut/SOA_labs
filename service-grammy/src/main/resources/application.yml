spring:
  application:
    name: service-grammy
  profiles:
    active: dev
#  config:
#    import: configserver:http://localhost:8888  # Config Server
#  cloud:
#    config:
#      fail-fast: false # если true приложение упадет если не будет конфига
  datasource:
    url: jdbc:postgresql://localhost:5432/studs
    username: s367826
    password: 4Xb2gijcYKqkn0xt
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true

server:
  port: 8444
  ssl:
    enabled: false
#    key-store: classpath:keystore.p12
#    key-store-password: yourpassword
#    key-store-type: PKCS12
cloud:
  consul:
    host: localhost
    port: 8500
    discovery:
      enabled: true
      register: false  # не регистрируемся в Consul
    config:
      enabled: false  # отключаем Consul Config
  service-registry:
    auto-registration:
      enabled: false  # отключаем авто-регистрацию через Consul


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

# LoadBalancer
spring.cloud.loadbalancer.enabled: false
ribbon:
  eureka:
    enabled: true  # по умолчанию Ribbon использует Eureka
  listOfServers: service-grammy:8444